# 🙋 СТАРТ НОВОЙ СЕССИИ: ПРОЧТИ ЭТО ПЕРВЫМ

🌸 **IRIS AI** | последнее обновление: 28.12.2025

---

## 1. ЧТО ПРОЧИТАТЬ СНАЧАЛА

Когда начинаешь новый чат или возвращаешься к проекту через время — начни с этих файлов:

1. **`docs/PROJECT_MASTER_CONTEXT.md`**  
   Кратко:
   - текущее состояние проекта;
   - архитектура и ключевые модули;
   - как запускается система;
   - что уже реализовано.

2. **`docs/DEV_LOG.md`**  
   Кратко:
   - история прошлых сессий;
   - что делали до тебя;
   - какие решения уже приняты.

3. **`docs/IRIS_PROJECT_VISION.md`** (при необходимости)  
   - общее видение проекта;
   - зачем всё это делается;
   - целевая картинка системы.

---

## 2. ЧТО УЖЕ РАБОТАЕТ

На текущий момент в приоритете считаем рабочим следующее ядро:

```bash
# Запуск (Windows, из папки iris_ai/)
cd iris_ai
python iris_complete_solution.py
```

Проверка API:

```bash
curl http://localhost:5000/health
# ожидается: {"status": "healthy", "iris": "ready"}
```

Если это работает — базовая система (мозг + сервер + GSI) считаются рабочими.

---

## 3. ЧТО НУЖНО ПРОТЕСТИРОВАТЬ

На момент последнего обновления ещё требуют проверки и доводки:

1. **🎤 Голос**  
   - распознавание речи (Vosk);
   - TTS (голоса, эмоции);
   - wake-word "Ирис";
   - прерывание речи (interrupt).

2. **🎮 CS2 GSI**  
   - корректность событий Kill / Death / Low Health;
   - правильное отображение контекста матча.

3. **📡 StreamElements**  
   - наличие и актуальность API-токенов;
   - подключение к чату/ивентам;
   - базовые сценарии реакции.

---

## 4. КОМАНДЫ ДЛЯ РАБОТЫ С ДОКУМЕНТАЦИЕЙ

### 4.1. Как читать контекст в новом чате

В начале любой новой сессии:

```text
1) Открыть:
   - docs/PROJECT_MASTER_CONTEXT.md
   - docs/DEV_LOG.md

2) Понять:
   - где остановились;
   - какие проблемы уже известны;
   - какие цели были на прошлую сессию.

3) Сформулировать цель текущей сессии и зафиксировать её.
```

### 4.2. Как обновлять журнал (DEV_LOG)

В конце каждой сессии добавь блок в `docs/DEV_LOG.md`:

```markdown
## 📅 [DD месяц YYYY]

### [HH:MM–HH:MM] MSK — [краткое описание]

**Что сделано:**
- …

**Проблемы / риски:**
- …

**Следующие шаги:**
- …
```

### 4.3. Как актуализировать проектный контекст

Если:
- добавлены новые ключевые модули;
- поменялась архитектура;
- изменились приоритеты;

обнови `docs/PROJECT_MASTER_CONTEXT.md` (разделы архитектуры и этапов).

---

## 5. ЧЕК-ЛИСТ ПЕРВЫХ 10 МИНУТ

1. Прочитать `docs/PROJECT_MASTER_CONTEXT.md` (архитектура и статус).
2. Прочитать последние записи в `docs/DEV_LOG.md`.
3. При необходимости — заглянуть в `docs/IRIS_PROJECT_VISION.md`.
4. Проверить, актуален ли `docs/CHECKPOINT.md`.
5. Определить одну–две конкретные задачи на текущую сессию.

---

## 6. РАБОЧИЙ ЦИКЛ

Нормальный цикл работы с проектом:

1. Начать с чтения контекста (файлы выше).
2. Сформулировать цель сессии и при необходимости вписать её в `docs/NEXT_SESSION_START.md` или DEV_LOG.
3. Делать изменения в коде/конфигурации.
4. При значимых изменениях — обновлять `docs/CHECKPOINT.md`.
5. В конце сессии:
   - обновить `docs/DEV_LOG.md`;
   - при необходимости — `docs/PROJECT_MASTER_CONTEXT.md`;
   - закоммитить и запушить изменения.

---

## 7. ЧТО КОПИРОВАТЬ В НОВЫЙ ЧАТ

Чтобы новый чат не начинался «с нуля», в начало промпта имеет смысл вставлять:

- содержимое `docs/00_MAIN_SYSTEM_PROMPT.md` (роль и правила работы);
- при необходимости — выдержки из:
  - `docs/CHECKPOINT.md`;
  - `docs/PROJECT_MASTER_CONTEXT.md`;
  - `docs/DEV_LOG.md`.

Так ИИ или новый разработчик сразу понимает контекст и может продолжать работу без переспрашивания.

---

**Главное правило:**
- не терять контекст;
- не оставлять изменения без отражения в документации;
- работать спокойно, последовательно и аккуратно.
