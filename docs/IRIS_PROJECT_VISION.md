# 💫 IRIS PROJECT VISION - ПОЛНАЯ АРХИТЕКТУРА АЙВИ

**Дата:** 27 декабря 2025  
**Народ** (то)н документа:** русский 🗣️  
**Фокус:** MVP → Production

---

## 1. ВИДЕНИЕ (ЧТО МЫ СТОИМ СТОИМ)

Не просто чат-бот. НЕ обыкновенный ассистент.

**IRIS = НОВАЯ ЛГРОВАЯ ГЕНЕРАЦИЯ, КОТОРАЯ:**
- НОВО понимает стримера (живые голосовые команды)
- НОВО анализирует игру (все активности в ороду времени)
- НОВО реагирует с смыслом (СДЕЛАНО через LLM, не скрипты)
- НОВО говорит живо (голос женщины, русский язык)
- НОВО с аудио еффектами (каждый ответ = аудио + голос)
- НОВО работает с шармом и характером (сарказм, юмор, поддержка)

**Конечный вид (VISION):** Стример делает kill → IRIS дает комментарий + звук + эмоция = НАГЛОЕ ВОЛШЕБСТВО 

---

## 2. АРХИТЕКТУРА MVP (МОДУЛИ)

```
┌───────────────────────────────────┐
│  🌸 IRIS MVP ARCHITECTURE                    │
└───────────────────────────────────┘

[🎙️ MICROPHONE] ---VOSK ASR (RU)---> [🗣️ TEXT]
                                    |
                                    ↓
                            [🧠 CONTEXT BUILDER]
                            (game events + conversation)
                                    |
                                    ↓
                        [💫 OLLAMA LLM (ru-friendly)]
                        Generate Russian response
                                    |
                                    ↓
                            [🔊 TTS ENGINE]
                        (Edge TTS / RHVoice RU)
                                    |
                                    ↓
                    [🔊 AUDIO OUT + 🔊 EFFECTS]
                        (Play to stream)

ПРАВА ГОЛосОВОЙ ПОТОк:
- KILL → on_kill() → яркая краткая фраза + звук
- DEATH → on_death() → дружеский троллинг
- CLUTCH → on_clutch() → эмоциональные фанфары
- LOW HP → on_low_hp() → ГОЛОСОВОЕ ПРЕДУПРЕЖДЕНИЕ
```

### 2.1. МОДУЛь 1: ASR (Audio Speech Recognition)

**Либрарея:** VOSK  
**Модель:** ru-RU (Russian)  
**Вход:** аудио с микрофона  
**Выход:** русские фразы

Код:
```python
import vosk
import pyaudio

def vosk_transcribe_ru():
    model = vosk.Model("model_ru")  # русская модель
    recognizer = vosk.KaldiRecognizer(model, 16000)
    
    # слушаем аудио, вовращаем текст
    return "[user_transcribed_russian_text]"
```

### 2.2. МОДУЛь 2: Context Builder

**Ответственность:** сборка контекста

Контекст = аргументы в LLM

Код:
```python
def build_context_prompt(user_text, game_context):
    """
    Пример:
    user_text = "Ак валю инфо?"
    game_context = {
        "recent_kill": True,
        "last_action": "ak_kill_3_enemies",
        "player_hp": 42,
        "round_score": "13-5"
    }
    
    то выдать русский prompt к LLM
    """
    prompt = f"""
    Ты - IRIS, голосовой ассистент CS2 стримера.
    Ответи живо, с сарказмом.
    
    Полюх события: {game_context}
    Ты только что забил {game_context['last_action']}
    
    Пользователь говорит: {user_text}
    
    Твоя ответ (одна короткая мудрая фраза):
    """
    return prompt
```

### 2.3. МОДУЛь 3: LLM (Ollama)

**Бэкенд:** Ollama на localhost:11434  
**Модель:** Qwen, Gemma или другая с русским  
**Скорость:** &lt;1s на CPU

Код:
```python
from ollama import Client

def generate_iris_response(prompt):
    client = Client(host="http://localhost:11434")
    response = client.generate(
        model="qwen2:1b",  # лёгкая модель
        prompt=prompt,
        stream=False,
        temperature=0.8  # для креативности
    )
    return response['response'].strip()
```

### 2.4. МОДУЛЬ 4: TTS (Text-to-Speech)

**Опционы:**
- Edge TTS (бесплатный, русские голоса)
- RHVoice (локальный)

Код:
```python
import edge_tts
import asyncio

async def iris_speak(text_ru):
    """
    text_ru = "Очко, не видел такого!"
    """
    communicate = edge_tts.Communicate(text_ru, voice="ru-RU-SvetlanaNeural")
    await communicate.save("iris_response.mp3")
    return "iris_response.mp3"  # вернём пать
```

### 2.5. МОДУЛь 5: Audio Effects + Playback

**Ответственность:** воспроизведение

Код:
```python
import pygame

def play_iris_with_sfx(voice_mp3, sfx_wav=None):
    pygame.mixer.init()
    
    # сначала SFX (опционально)
    if sfx_wav:
        sfx = pygame.mixer.Sound(sfx_wav)
        sfx.play()
        pygame.time.wait(int(sfx.get_length() * 1000))  # ждём
    
    # затем голос
    voice = pygame.mixer.music.load(voice_mp3)
    pygame.mixer.music.play()
    while pygame.mixer.music.get_busy():
        pass
```

---

## 3. MVP ПАЙПЛАЙН (МОМЕНТАЛЬНО)

### Шаг 1: Которые тесты должны пройти за день 1

- [ ] test_vosk_ru.py - ASR работает
- [ ] test_ollama_connection.py - Ollama отвечает
- [ ] test_edge_tts.py - TTS генерирует русские аудио

### Шаг 2: интеграция в iris_brain.py

```python
class IrisAI:
    def __init__(self):
        self.vosk_model = load_vosk_ru()
        self.ollama_client = Client("http://localhost:11434")
        self.context = {"last_kills": [], "events": []}
    
    def listen_and_respond(self):
        # Листен
        user_text = vosk_transcribe_ru()
        
        # Накомпилируем промпт
        prompt = build_context_prompt(user_text, self.context)
        
        # Генерируем ответ
        response_text = self.ollama_client.generate(model="qwen2:1b", prompt=prompt)
        
        # Синтезируем с TTS
        audio_file = await iris_speak(response_text)
        
        # Проигрываем
        play_iris_with_sfx(audio_file)
```

### Шаг 3: На CS2 события

```python
def on_kill_detected(victim_name, weapon):
    # триггер когда игрок килл
    iris.react_to_kill(victim_name, weapon)
    # это вапним быстро, короткая реплика + звук
```

---

## 4. РУССКОГОВОРЯЩНЫЕ ПРОМПТЫ ДЛЯ LLM

### Промпт МАГИЧЯНИУ

```python
SYSTEM_PROMPT_RU = """
Ты - IRIS, голосовой ассистент для CS2 стримера.

характер:
- иронична, саркастична, дружелюбна
- слегка токсичная, но без жёстоких оскорблений
- всегда на русском языке

Ответы:
- Короткие (не более 1-2 предложений)
- Естественные, как настоящий голос
- динамичные, а не скучные

После kill:
  - "Комбо, я ни сказала!"
  - "Конечно, ты всё гений!"
  - "Опять kill? У меня эта бансттуку!"

После death:
  - "Знав, кто-то же читер..."
  - "Ладно, это было вынужденно. На втором раунде выверем вы всех!"

Покрайне LOW HP:
  - "У тебя трава! Колдывай на кунгфу!"
  - "Единорог, ниста! Очки пономи, хенсню, бежи!"
"""
```

---

## 5. ТИМИНГОВКА ДО ПОЛНОМУ MVP

| Фаза | Время | Что | Готово |
|--------|--------|------|--------|
| **Day 1** | 2-3 hours | Контроль VOSK ASR | Модуль готов |
| **Day 2** | 2-3 hours | Контроль Ollama LLM | Модуль готов |
| **Day 3** | 2-3 hours | Контроль Edge TTS | Модуль готов |
| **Day 4** | 3-4 hours | Интеграция + тест | MVP работает! |

---

## 6. ОТ MVP к Production

**После MVP готов, доработка (ГЕОДЕЗИИ размер):**

1. **Week 1**: Система достижений + деполоймент
2. **Week 2**: Остальные гам-события (бомба, батл, clutch...)
3. **Week 3**: Стрим-евенты (donors, subs, raids)
4. **Week 4**: Полировка + release

---

## 7. КЛЮЧЕВОЕ

**ТАК КАК НУИ И РУССКОГОВОРЯЩНАЯ:**

- Твое Код = ЛОКАЛЬНАЯ
- Всё реплики IRIS = РУССКОГОВОРЯЩИЕ
- Бесплатно!

**Одна и таже: характер, юмор, магия.**

---

**Статус:** 1.0  
**Готово к продуктивности!** 🔥
