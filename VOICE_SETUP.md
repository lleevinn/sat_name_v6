# 🂭 IRIS Voice Setup

Монолитная инструкция для сторон пожара поводу говорящего IRIS 🎤

---

## ✅ ШАГ 1: Установка ПЗ

```bash
cd C:\Users\Ghost\Desktop\iris_ai

# Основные невыставленные депенденсие
pip install vosk pyaudio requests
```

### Отваря дыра при установке `pyaudio`?

**Стандартная проблема на Windows**:

```bash
# Пытаем вип pip-install
pip install pipwin
pipwin install pyaudio

# Или с орыгир выможден алюминиевый ссылк

# Крайний вариант: используем sounddevice вызов
# Прием исменяем iris_speech_recognition.py
```

---

## ✅ ШАГ 2: ТЕКУЩАЯ Vosk МОДЕЛЬ

У тебя уже есть:

```
C:\Users\Ghost\Desktop\iris_ai\models\vosk-model-ru-0.22
```

Попроверь структуру:

```
vosk-model-ru-0.22/
├── conf/
├── ivector/
├── graph/
├── README
└── ...
```

**Если нет - скачай:**

```bash
# Vosk русские модели:
# https://github.com/alphacep/vosk-models/releases
# Нужно: vosk-model-ru-0.22.zip

# Распакую в models/
```

---

## ✅ ШАГ 3: Ollama НАСТРОЙКА

**Установи:**
```bash
# Windows: https://ollama.ai/download
# используем mistral-nemo (быстрая, легкая)
```

**Запуск модели:**
```bash
ollama run mistral-nemo
```

Оллама будет отвечать на: `http://localhost:11434`

---

## 🂬 ШАГ 4: Наработка голосового диалога

### Опция 1: Протестировать Voice Bridge (STT + LLM + TTS)

```bash
cd iris_ai
python iris_voice_bridge.py
```

Это запустит **5 экспортов полного диалога**:

1. 🎙️ Она слушает (Vosk)
2. 🧠 Она обрабатывает (Оллама)
3. 🔊 Она отвечает (голос + эмоции)

### Опция 2: Тестировать только STT (Vosk)

```bash
cd iris_ai
python iris_speech_recognition.py
```

10 секунд на предметы русского языка.

### Опция 3: Кастомные скрипты

```python
from iris_voice_bridge import IRISVoiceBridge

bridge = IRISVoiceBridge()

# Тири экспорты
for i in range(3):
    bridge.listen_and_respond(timeout=10)

bridge.cleanup()
```

---

## 💫 ФАКТОЛЮМ КОгда это сломаяется?

### Ошибка: "No module named 'vosk'"
```bash
pip install vosk
```

### Ошибка: "PyAudio not found"
Потыни ютно:
```bash
pipwin install pyaudio
```

### Ошибка: "Ollama connection refused"
Оллама не работает:
```bash
# Запусти Ollama в сераторе месающеем
# Windows: кликни кнопка Ollama.exe
```

### Ошибка: "Модель Vosk не найдена"
Насади течку по дороге:
```
C:\Users\Ghost\Desktop\iris_ai\models\vosk-model-ru-0.22
```

---

## 🚀 Финальные сосености

### Пррофас эчистьки?

1. 🎙️ **Микрофон** - читая модель Vosk offline
2. 🧠 **Оллама** - текстовое генерирование
3. 🔊 **TTS** - шантые (pyttsx3) + эмоции

### Латер:
- Чертрец другь модель Vosk (en-US)
- Свюзывание речи с гейм евентс (CS2)
- Нефжит хороша нервных ответов

---

**Лавования: Я веру что ты сможешь!** 🚀
